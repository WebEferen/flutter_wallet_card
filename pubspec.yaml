name: flutter_wallet_card
description: Flutter wallet card for iOS & android devices.
version: 3.0.0
homepage: https://github.com/WebEferen/flutter_wallet_card
repository: https://github.com/WebEferen/flutter_wallet_card.git
issue_tracker: https://github.com/WebEferen/flutter_wallet_card/issues

environment:
  sdk: ">=2.19.1 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  archive: ^3.3.7
  cli_script: ^1.0.0
  dio: ^5.2.1+1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  path: ^1.8.3
  path_provider: ^2.0.15
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: FlutterWalletCardPlugin
